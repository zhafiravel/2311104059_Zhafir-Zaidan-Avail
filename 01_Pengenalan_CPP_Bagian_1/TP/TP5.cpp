#include <iostream>

using namespace std;

int main() {
  // Deklarasi variabel
  int nilai;

  // Meminta pengguna untuk memasukkan nilai
  cout << "Masukkan nilai: ";
  cin >> nilai;

  // Pengecekan kondisi
  if (nilai > 80) {
    // Jika nilai lebih dari 80, cetak "A"
    cout << "A" << endl;
  } else {
    // Jika tidak, cetak "Bukan A"
    cout << "Bukan A" << endl;
  }
    cout << "Zhafir Zaidan Avail" << endl;
  return 0;
}
